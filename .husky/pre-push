#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Redirect stdin to /dev/tty
# exec < /dev/tty

set -x

yarn patch && git add docs/ && git commit -m "deploy: v$(node -p "require('./package.json').version")"
