#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Redirect stdin to /dev/tty
# exec < /dev/tty

set -x

npm version patch --no-git && yarn build && git add docs/ && git commit -m "deploy: v$(node -p "require('./package.json').version")"
